---
name: organize-markdown-images
description: 整理 Markdown 文档中的图片：扫描 .md 文件里的图片引用，把图片统一复制到 "图片/<文档标题>/" 目录并按顺序重命名为 01-、02-，再把 markdown 引用改成相对路径。当用户提到整理 markdown 图片、统一图片目录、改为相对路径、准备网页文档时触发。
---

# 整理 Markdown 图片

## 何时使用

- 整理 markdown 文档中的图片
- 把图片放到统一文件夹
- 把图片路径改成相对路径
- 准备复制到网页做文档
- 规范化 markdown 图片引用

## 操作步骤

1. 扫描当前目录（或指定目录）下的所有 `.md` 文件。
2. 提取 `![alt](path)` 形式的图片引用。
3. 对每张本地图片：
   - 在 markdown 文件所在目录创建 `图片/<文档标题>/` 子目录
   - 按出现顺序命名为 `01-`、`02-` ……
   - 文件名使用 alt 文本或原文件名
4. 更新 markdown 中的图片引用为新的相对路径。
5. 网络图片 URL 保持不变，缺失图片在报告中说明。

## 报告模板

```
处理的 Markdown 文件数：
复制的图片数：
缺失图片数：

文件 | 原路径 | 新路径 | 状态
...  | ...    | ...    | 成功/缺失/跳过
```

## 使用脚本

运行 bundled 脚本完成操作：

```bash
python scripts/organize_images.py <markdown文件或目录>
```

默认扫描当前工作目录。
